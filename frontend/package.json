{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "",
    "build:dev": "rm -rf ../assets/public/* && parcel build --no-optimize --no-cache --no-content-hash src/index.html --dist-dir ../assets/public --public-url .",
    "watch": "rm -rf ../assets/public/* && parcel watch --no-cache --no-content-hash src/index.html --dist-dir ../assets/public --public-url ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.2"
  }
}